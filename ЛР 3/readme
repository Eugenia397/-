1) БИНАРНАЯ КУЧА
ОПРЕДЕЛЕНИЕ: Это дерево, удовлетворяющее свойствам полноты (полностью заполненные уровни сверху вниз, последний уровень слева направо) и упорядоченности (родительские узлы больше или меньше дочерних).
Используется преимущественно для реализации приоритетных очередей.

АНАЛИЗ СИНТАКСИСА 
- Python: Встроенная поддержка обеспечивается модулем heapq. Можно также реализовать вручную, создавая список и применяя стандартные методы вроде heappush() и heappop().
Пример задания бинарной кучи: 
import heapq
min_heap = []
heapq.heappush(min_heap, 10)   # push operation
heapq.heappush(min_heap, 5)    # push another item
print(heapq.heappop(min_heap)) # pop the smallest item

- Java: Встроенный способ организации кучи осуществляется с помощью класса PriorityQueue<T>.
Пример задания бинарной кучи:
import java.util.PriorityQueue;PriorityQueue<Integer> minHeap = new PriorityQueue<>()
minHeap.offer(10);      // Добавление элемента
minHeap.offer(5);       // Добавление другого элемента
System.out.println(minHeap.poll()); // Удаление наименьшего элемента

- C++: Подключение контейнера priority_queue позволяет легко создать максимальную или минимальную кучу.
Пример задания бинарной кучи:
#include <queue>
#include <iostream>
std::priority_queue<int> maxHeap;     // По умолчанию максимальная куча
maxHeap.push(10);                    // Добавление элемента
maxHeap.push(5);                     // Добавление второго элемента
std::cout << maxHeap.top() << '\n';  // Печать наибольшего элемента



2) БИНОМИНАЛЬНАЯ КУЧА
Определение: Совокупность биномиальных деревьев, каждая вершина которых имеет структуру дерева, состоящего из узлов одной степени.
Обеспечивает эффективную реализацию операций слияния и вставки элементов.



3) КУЧА ФИБОНАЧЧИ
Определение: Совокупность ориентированных деревьев, каждое из которых является корневым деревом.
Отличается лучшей временной сложностью асимптотически оптимизированных операций удаления минимального элемента и уменьшения ключа узла.



4) ХЭШ-ТАБЛИЦА
Определение: Структура данных, обеспечивающая быстрый доступ к элементам путем вычисления хэша ключей и последующего хранения значений в соответствующих ячейках массива.
Поддерживает операции добавления, удаления и поиска за среднее время O(1).


